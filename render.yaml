services:
  - type: web
    name: di-vision-iso
    env: python
    plan: free
    buildCommand: ./build.sh
    startCommand: python manage.py migrate && gunicorn safety_mvp.wsgi:application
    envVars:
      - key: DEBUG
        value: false
      - key: PYTHON_VERSION
        value: 3.11.5
